# Architectural Decisions — __PROJECT_NAME__

## Data layer
Supabase pgvector over Pinecone — cost + latency at scale

## Auth
Supabase Auth — single vendor, RLS native

## Forms / mutations
Server Actions over Route Handlers — less boilerplate, type-safe

## Components
shadcn/ui copied — full control, no upstream lock-in
